name: make-release
run-name: Make release

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: make ci_build_64tass
        
